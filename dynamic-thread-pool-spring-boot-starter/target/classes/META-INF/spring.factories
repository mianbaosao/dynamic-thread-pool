org.springframework.boot.autoconfigure.EnableAutoConfiguration=\
  cn.bread.middleware.dynamic.thread.pool.sdk.config.DynamicThreadPoolAutoConfig
#这一行配置的主要目的是在 Spring Boot 启动时确保加载 DynamicThreadPoolAutoConfig，从而初始化动态线程池的设置。
# 这样，应用可以使用动态线程池功能，而无需手动在代码中配置线程池相关的 Bean。